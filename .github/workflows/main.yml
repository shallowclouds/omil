name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    uses: ./.github/workflows/shared.yml
    with:
      target_path: /github-actions/omil/unstable
    secrets: inherit
